# @formspec/dsl-policy

Private internal FormSpec package for Chain DSL policy types, defaults, and validators.

This package is a workspace implementation detail. It is not published, has no API Extractor report, and should not appear as an install-time dependency of published FormSpec packages. Application authors should import DSL-policy configuration APIs from `@formspec/config`, which exposes the supported public compatibility surface.
