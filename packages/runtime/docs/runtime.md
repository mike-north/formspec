<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@formspec/runtime](./runtime.md)

## runtime package

`@formspec/runtime` - Runtime helpers for FormSpec

This package provides utilities for working with FormSpec forms at runtime: - `defineResolvers()` - Type-safe resolver definitions for dynamic enum fields

## Example

```typescript
import { defineResolvers } from "@formspec/runtime";
import { formspec, field } from "@formspec/dsl";

// Define a form with dynamic enum fields
const form = formspec(field.dynamicEnum("country", "countries", { label: "Country" }));

// Define resolvers for the form's data sources
const resolvers = defineResolvers(form, {
  countries: async () => ({
    options: [
      { value: "us", label: "United States" },
      { value: "ca", label: "Canada" },
    ],
    validity: "valid",
  }),
});

// Use the resolver
const result = await resolvers.get("countries")();
console.log(result.options); // [{ value: "us", ... }, { value: "ca", ... }]
```

## Functions

<table><thead><tr><th>

Function

</th><th>

Description

</th></tr></thead>
<tbody><tr><td>

[defineResolvers(form, resolvers)](./runtime.defineresolvers.md)

</td><td>

Defines resolvers for a form's dynamic data sources.

This function provides type-safe resolver definitions that match the form's dynamic enum fields.

</td></tr>
</tbody></table>

## Interfaces

<table><thead><tr><th>

Interface

</th><th>

Description

</th></tr></thead>
<tbody><tr><td>

[ResolverRegistry](./runtime.resolverregistry.md)

</td><td>

A resolver registry that provides type-safe access to resolvers.

</td></tr>
</tbody></table>

## Type Aliases

<table><thead><tr><th>

Type Alias

</th><th>

Description

</th></tr></thead>
<tbody><tr><td>

[ExtractDynamicSources](./runtime.extractdynamicsources.md)

</td><td>

Extracts dynamic data-source names referenced by a single FormSpec element.

</td></tr>
<tr><td>

[ExtractDynamicSourcesFromArray](./runtime.extractdynamicsourcesfromarray.md)

</td><td>

Extracts dynamic data-source names referenced anywhere in an element array.

</td></tr>
<tr><td>

[Resolver](./runtime.resolver.md)

</td><td>

A resolver function that fetches options for a data source.

</td></tr>
<tr><td>

[ResolverMap](./runtime.resolvermap.md)

</td><td>

Map of resolver functions for a form's dynamic data sources.

</td></tr>
<tr><td>

[ResolverSourcesForForm](./runtime.resolversourcesforform.md)

</td><td>

Derives the resolver source-key union for a FormSpec element array.

</td></tr>
</tbody></table>
