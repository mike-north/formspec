"@formspec/build": minor
"@formspec/cli": minor
---

Preserve enum member display-name annotations in the static analysis pipeline so
schemas emit per-member `title` values via `oneOf` entries for
`@displayName :member Label` syntax.
