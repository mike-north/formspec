---
"@formspec/eslint-plugin": patch
---

Allow numeric constraints on optional and nullable registered custom types, including nullable array items, while preserving support for imported branded numeric types.
